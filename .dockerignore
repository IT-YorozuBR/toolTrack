# Dependências e artefatos de build (reinstalados/recriados dentro da imagem)
node_modules
.next
out
build
coverage

# Versionamento e configs de editor/CI
.git
.gitignore
.github
.vscode
.idea

# Variáveis de ambiente locais — NUNCA entram na imagem (vêm via .env em runtime)
.env
.env.*
!.env.example

# Claude / worktrees / ferramentas locais
.claude
.vercel

# Logs e temporários
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
*.tsbuildinfo

# Docker (não precisa no contexto de build)
Dockerfile
.dockerignore
docker-compose.yml

# SO
.DS_Store
